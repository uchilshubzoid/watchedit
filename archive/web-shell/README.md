# Archived Web Shell

This folder contains the deprecated Create React App shell from Stage 1.

It is kept only as historical/migration reference. The active app is the Expo React Native app in `app/` and `src/`.

Do not add new product work here.
